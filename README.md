# running-game

This repository is configured to publish `index.html` to GitHub Pages.

## GitHub Pages

- Deployment is handled by `.github/workflows/deploy-pages.yml`.
- On every push to `main`, the site is deployed automatically.
- The site will be available at:
  - `https://konradknitter.github.io/running-game/`
